spring:
  application:
    name: sy-image-search-mcp
  profiles:
    active: ${MCP_SPRING_PROFILES_ACTIVE:sse}
server:
  port: 8127

# Pexels 图片搜索 API（敏感信息请用环境变量 PEXELS_API_KEY 或 application-local.yml）
pexels:
  api-key: ${PEXELS_API_KEY:}
